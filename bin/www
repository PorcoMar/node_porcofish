const app = require('../app');
const http = require('http');
let port = require('../config/config.js').port;

let server = http.createServer(app);
server.listen(port, () => {
	console.log('listen serever port is ', port)
})


